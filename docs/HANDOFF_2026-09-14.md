# 交接备注｜2026-09-14

## 本次完成

- 一键生成创作包增加 AI 语音标注与 AI Mask 标注开关；仅自动模式生效，关闭后对应阶段跳过并正确记录进度。
- 模型改为全局复用的模型库，创作包仅负责关联；模型和创作包均支持安全删除，历史项目仍使用内部快照。
- 修复静态整页场景被历史 12 秒动画值强制拉长的问题：无 Reveal 动画时按真实音频时长加收尾时长。
- 重做创作配置包管理：单一名称字段，已有包四列紧凑未选中卡片，只保留名称、编辑、复制、删除；名称可修改，另存直接使用当前名称。
- 首页账号切换展示“默认账号”，改为平铺列表并增加当前账号选中态；课程树新增行取消左右描边，课程添加与更多操作列对齐。
- 按用户确认清理模型库：文本、图片、语音各只保留最早的迁移模型；其余 21 条已归档，从后续绑定列表移除。

## 当前保留模型

- 文本：旧版文本模型（迁移）
- 图片：旧版图片模型（迁移）
- 语音：旧版 MiniMax 语音（迁移）

## 本地运行

```powershell
py -3.13 -m uvicorn server:app --host 127.0.0.1 --port 8003
```

访问地址：`http://127.0.0.1:8003/`。

## 已执行验证

```powershell
node --check static/creation_config_management.js
node --check static/account_management.js
node --check static/courses.js
node checks/test_frontend_quality.js
py -3.13 -m pytest checks/test_creation_config.py checks/test_creation_config_defaults.py checks/agent/test_contract_model_parity.py -q
py -3.13 scripts/generate_agent_contracts.py --check
git diff --check
```

上述检查通过；pytest 为 26 项通过（含一条第三方弃用警告）。

## 后续建议

1. 接入新图片中转能力：比例、分辨率、质量、提示词、参考图与异步状态。
2. 接入语音参考音频与固定润色提示词，并在提示词板块公开规则。
3. 使用真实项目完整走一遍自动模式，复核图片、Mask、旁白和最终导出。
