# 阶段 A（raw/normalized 双图管线）真实 API 盲测复跑（2026-09-19）

与 `../ai_blind_p1` 同一隔离配方：:8011 单实例（DB 快照 + 临时 runs，未触碰真实项目），
专用测试项目 `9e8168fd_232846`，按 `dataset.json` 冻结 11 页（8 页旧集 + E1 新增 3 页）
上传 → Step3 确认 → `POST /steps/5/ai-mask/annotate`
（`{"settings":{"fine_grained_detection":true}}`）→ 导出真实 Manifest RLE → 包内评分器评分。
annotate 用时 318.8s；11 页 `mask_source` 全部为 `raw_pair`（Stage A 生效的直接证据）。

## 与优化前（`../ai_blind_p1/on`，P1-on2，无 raw 对）同口径对照（旧 8 页）

| 指标 | P1-on2（优化前） | P1+raw（本次） |
| --- | --- | --- |
| 标题组 recall（01–08 各页 group_001） | ≈0.9523 | **0.9962** |
| 05_pale 前景覆盖率 | 0.1109（浅灰底板整片被上传洗白） | **0.9999** |
| 05_pale pale_recall | 0.0345 | **0.9999** |
| 08_detached 覆盖率 | 0.9695（未过 0.995 门） | **0.9969（过门）** |
| 01/02/03/04/06/08 评分 | 全部 fail（洗白像素计入缺口） | **全部 pass** |
| 8 页过门数 | 0/8 | **6/8** |
| 重叠像素 | 0 | 0 |

E1 新增页：09_pale_nested_combo、11_halo_lines 直接 **pass**（cov/pale=1.0）；
10_dense_20 检测层质量全过（覆盖 1.0、重叠 0），但端到端 fail，归因见下。

## 残余失败归因（对逐组数字负责）

1. `07_nested`：VL 仍把公共外框切片配给 group_002，标题 recall 0.3308——与优化前
   （0.3162）同类语义匹配错误，属阶段 C（局部拆分/复核协议）范围，非 Stage A 回归。
2. `10_dense_20`：第 1 页 VL 返回截断 JSON（`JSONDecodeError`），确定性回退只锚定
   12/21 组，9 组无 RLE；导出器对缺 RLE 组整体抛错、评分按空计（cov=0）。检测层
   本身 21 组件全部独占归属通过。属阶段 C 未匹配组协议 + 供应商输出稳定性范围。
3. `05_pale`：过门外仅剩 group_002/003/004 precision ≈0.973 的跨组浅底边界归属
   （2.7% FP），属阶段 D 边界细化范围；已从"整片丢失"收敛到"边界精度"量级。

## 结论

阶段 A 的预期杠杆全部兑现且可量化：上传洗白门对 Mask 路径的影响被 raw 对消除，
标题 recall 与 05 浅灰恢复达到检测层 oracle 水平，8 页盲测过门数 0→6；剩余失败
全部落在 C/D 阶段既有归因内，无新增回归。
