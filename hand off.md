# hand off

## 配置体系、创作流程与 Agent 对接代码审查报告

- 审查日期：2026-09-16。
- 代码基线：`66c38c8`（报告编写前与远端 `main` 一致）。
- 审查目标：核实单一当前配置、模型与创作包引用、流程风险及 Agent 对接状态，给出分级问题、影响和整改验收标准。
- 本次交付是审查报告；下列整改建议尚未实施。

## 一、结论

目前不能认定代码已经完全满足“模型和创作设置均无历史版本、始终使用当前配置”的要求。

模型和创作包的注册表已经把历史集合收敛为一个当前值，更新时覆盖该值。但是旧版本接口、字段和项目配置副本仍然存在。尤其是创作包删除实际采用归档，运行中的项目又要求解析该包的当前值，两者存在冲突，可能导致旧项目无法继续运行。

Agent 并非已经全面无法对接：此前针对 Agent 和配置的自动测试通过，合同生成检查通过。准确结论是“传输与部分接口契约仍可用，但部分参数语义和单一当前配置产品规则不一致”。是否保留 Agent 应由实际使用需求决定，不能根据遗留字段直接推断整个接口失效。

本次未发现并验证可直接定为 P0 的问题。优先处理 P1 的配置删除与运行解析冲突，以及配置文件异常时的隐式回退；随后完成单一当前配置的接口、存储和客户端清理。

## 二、范围与证据边界

检查覆盖：模型连接注册表、创作包解析与删除、项目创建及运行配置、主一键流程预检、Agent 请求模型/路由/客户端/认证，以及相关自动测试。

主要源码：

- `model_connection_service.py`、`model_connection_routes.py`。
- `creation_config_service.py`、`creation_config_routes.py`、`creation_config_models.py`。
- `project_service.py`、`project_config_runtime.py`、`reusable_config_startup.py`。
- `one_click_orchestrator.py`。
- `agent_contract/`、`agent_api/`、`agent_client/`、`app_security.py`。

证据分为源码确认、此前本次审查期间的运行数据观察、自动测试和待实测风险。自动测试通过不等于所有第三方服务、账号额度、显存、工作流和完整视频链路都已实测通过。本次未重新执行付费图片/音频请求，也未重新生成完整 MP4 或逐页浏览 UI。

## 三、问题分级总表

| 编号 | 优先级 | 分类 | 问题 | 证据状态 |
| --- | --- | --- | --- | --- |
| F01 | P1 | 正确性 | 创作包归档后，引用它的旧项目可能无法解析运行配置 | 源码路径确认；未操作真实项目删除 |
| F02 | P1 | 正确性 | 当前配置解析仍以前置项目快照文件有效为条件 | 源码路径确认 |
| F03 | P2 | 产品一致性 | 注册表单值化，但旧版本接口、字段和快照仍存在 | 源码确认 |
| F04 | P2 | 生命周期 | 模型删除仅归档，仍保留记录和历史语义 | 源码确认 |
| F05 | P2 | 故障前移 | 已绑定 ComfyUI/IndexTTS 模型的项目跳过对应全局预检分支 | 源码确认 |
| F06 | P2 | Agent | 版本与默认参数语义仍有遗留 | 源码及合同测试证据 |
| F07 | P2（条件性） | 安全/部署 | 无 Agent 凭据时存在开发模式，部署须核实完整访问控制 | 源码确认；不是已复现远程越权 |
| F08 | P3 | 可维护性 | 快照、版本、数字人配置能力的说明与实际行为需统一 | 源码/仓库规范对照 |

P1：可阻断已有项目或导致配置来源错误，优先修复。P2：需求不完整、故障延迟或部署条件风险。P3：维护和交接成本问题。

## 四、详细问题与整改建议

### F01 / P1：创作包删除策略与运行解析冲突

依据：`creation_config_routes.py` 的删除路径及注释表示保留历史项目快照；`creation_config_service.py::resolve_creation_config` 对归档包抛出 `CreationConfigConflict`；`project_config_runtime.py::load_project_config` 对绑定包优先解析当前值，普通解析异常转为 `ProjectConfigBindingError`，仅注册表未配置的 `RuntimeError` 分支使用旧文件回退。

触发：项目引用某创作包 → 删除/归档该包 → 旧项目再次读取运行配置。

影响：旧项目文件虽然还在，但继续生成可能被“当前创作设置不可用”阻断。“保留快照”不能证明旧项目可继续运行。

建议：按用户要求采用当前引用策略：被项目引用的包禁止删除，返回引用清单；用户先改绑项目，最后删除。无引用的包可真正删除。归档如保留，必须明确其是否允许已有引用继续读取，不能同时承诺旧项目无影响又拒绝解析。

验收：覆盖被引用删除、改绑后删除、未引用删除、已归档包的旧项目读取；UI 提示与 API 结果一致。

### F02 / P1：快照文件异常会提前退出当前配置解析

依据：`project_config_runtime.py::load_project_config` 在查 `creation_config_package_id` 之前，先检查 `planning/project_config.json` 的存在、大小、JSON、payload 和 schema_version；任一失败直接返回 `None`。

触发：项目数据库有有效创作包引用，但项目配置文件丢失、损坏或格式不满足旧约定。

影响：有效当前绑定不被读取；调用方可能按无配置项目处理并回退全局设置，造成模型、账号或参数来源与用户所选不一致。这是代码支持的失败路径，不代表此前 403 已证实由它引起。

建议：先按项目所属账号与包 ID 解析当前配置。只有没有绑定的明确旧项目才进入兼容回退；有绑定但不可用应明确报错，不能静默换模型。

验收：删去或损坏测试项目配置文件，仍能使用有效当前绑定；无效绑定返回清晰错误；无绑定旧项目保留明确兼容行为。

### F03 / P2：单一当前配置改造未贯穿接口与存储

依据：`creation_config_service.py::_store` 和更新路径收敛为 `versions={"1": current}`；`model_connection_service.py` 把 revisions 收敛为一个 revision=1。但 `creation_config_routes.py` 仍公开 GET/POST `/versions`；`creation_config_models.py`、`project_service.py`、Agent 请求模型仍带 version/revision；项目创建仍写 `planning/project_config.json`，并保存版本/hash 字段。

影响：当前并非真的持续创建多个历史版本，但外部调用者仍可能以为指定旧版本可固定旧配置。实际解析忽略旧版本，接口含义与行为不一致；项目副本也继续造成“到底哪个配置生效”的理解成本。

建议：模型和创作包各保留稳定 ID 与当前配置。删除历史版本 API，或明确限期弃用并拒绝非当前版本参数；同步前端、Agent、文档和测试。项目仅保留必要引用，运行产物元数据与可执行配置历史分离。若清理数据库字段，新增连续迁移，不修改已执行迁移。

验收：同类型可建多个独立配置；修改其中一个后所有引用按规则读到当前值；不能通过旧版本号取回历史配置；修改不影响其他独立配置。不要误删 Agent API 协议版本、数据库迁移号和产物管线版本，它们不是用户配置历史。

### F04 / P2：模型删除始终归档

依据：`model_connection_service.py::delete_model_connection` 把状态设置为 archived，响应包含 `retained_for_history=True`，没有按是否被引用选择物理删除。

影响：用户看到“删除”，后台仍保留记录；与“不要历史记录”的要求不一致。归档模型的可解析策略也需要与创作包统一。

建议：引用中的模型提示哪些创作包依赖它，并阻止直接删除；无引用模型真正删除。凭据清理要核实是否共享，避免影响其他连接。

验收：引用保护、改绑、无引用删除、凭据共享与独占清理均有覆盖，界面名称与实际行为一致。

### F05 / P2：绑定模型路径缺少同等强度的本地 TTS 预检

依据：`one_click_orchestrator.py::_preflight_errors` 仅在 `not tts_bound and provider == "comfyui_tts"` 时检查全局工作流文件并调用 inspector。

影响：项目通过创作包绑定 IndexTTS 时，不能依赖该分支在流程开始前发现本地服务、节点或工作流缺失。可能先完成耗时步骤，才在音频生成时报错。

建议：先解析实际生效 TTS 连接，再按 provider 执行同一套预检，读取该连接自己的端点、工作流和参考音频，不取无关全局默认值。预检成功不应被描述为保证不爆显存。

验收：绑定与未绑定两条入口分别测试服务停止、工作流缺失、节点缺失、参考音频缺失和正常状态；诊断指出实际使用的连接，隐藏密钥。

### F06 / P2：Agent 可调用，但产品语义有漂移

依据：`agent_contract/models.py` 和 `agent_api/routes.py` 仍接受/转发 `config_package_version`；能力描述仍有 versioned creation configuration；`agent_client/client.py::create_project` 的 `mask_enabled=True` 默认值与服务器按 presentation_mode 决定的语义需要统一。

影响：合同测试可证明 API/MCP/CLI 等声明的部分一致性，不能证明“无配置版本”等产品需求全部符合。调用者可能以为版本或 mask 参数有效，实际被忽略或由其他字段决定。

建议：若近期不用 Agent，先增加默认关闭开关并明确停用文档，再按实际调用关系决定移除哪些模块。若保留，统一当前包引用和展示模式字段，更新能力版本、API 协议版本、生成文档与合同测试。不能只删除一组路由却保留活跃客户端和 UI 入口。

验收：同一配置从页面与 Agent 创建项目时，实际模型及流程结果一致；各支持参数有行为测试；停用模式不再暴露相关能力。现有 AGENTS.md 要求 Agent 同步，正式移除时需一并更新该规范。

### F07 / P2（条件性）：Agent 开发模式需明确部署边界

依据：`agent_api/auth.py` 在既无旧式环境凭据也无存储账号令牌时允许开发模式，并接受账号选择头；`server.py` 默认装配 Agent；`app_security.py` 另有整站访问控制。

影响：若部署可被非可信调用者访问，又未配置有效认证，开发模式可能不符合预期账号隔离。不能仅凭 Agent 中间件代码断言整站已存在远程绕过，因为还有整站认证、来源和主机限制。

建议：保留本机开发便利的同时明确非本机部署要求，增加覆盖完整中间件组合的认证测试；Agent 如不使用则默认关闭。

验收：本地开发、远程无凭据、错误凭据、正确账号令牌、跨账号操作都通过真实应用路由验证，拒绝路径不执行业务操作。

### F08 / P3：说明与可选数字人能力需要明确

代码仍出现 immutable revision、snapshot、历史保留等描述，应随 F01–F06 同步改写，避免维护者再引入旧配置语义。

`digital_human` 出现在创作包允许字段中，但本次没有确认其完整校验与消费链路。应继续核实各字段是否真正影响数字人作业；未消费字段应删除或明确标为未支持。

仓库规范明确数字人为可选步骤，不阻塞作品输出。因此，一键主阶段没有数字人生成阶段本身不是 bug。若期望一键同时生成数字人，这是需要明确启用条件、失败策略、进度与输出合成规则的功能扩展。

## 五、运行配置观察

此前本次审查读取本机运行目录的数据时，模型注册表各连接已只有一个 revision，默认创作包也只有一个当前 version。旧项目仍保留版本 1、2、5 等值。这是迁移残留证据，不证明运行仍按这些版本调用。

当时默认创作包的 TTS 绑定为“默认豆包音频生成 1.0”，而“本地 ComfyUI / IndexTTS 2.5”是另一个可选模型。模型存在不等于默认包已选择它。该观察有时间边界；实际切换前应重新读取当前绑定，不要直接覆盖用户后来作出的选择。

此前图片供应商返回的 `INSUFFICIENT_BALANCE` 表示请求被供应商以额度不足拒绝。当前审查没有足够证据把它归因于版本残留；应追踪失败请求实际使用的连接 ID、供应商和模型，并脱敏记录。`provider_not_allowed` 属于图片来源确认规则，不能与账户余额错误合并解释。

## 六、逐步流程评估

| 可见步骤 | 评估 | 后续重点 |
| --- | --- | --- |
| 导入文章 | 本次无新增已确认专属缺陷 | 验证当前文本连接和空项目入口 |
| 分镜规划 | 受当前包解析链影响 | F01/F02；文章修改后的下游失效 |
| 图片生成 | 不能用测试通过证明供应商余额与实际请求可用 | 脱敏追踪实际连接；生成后来源确认闭环 |
| AI Mask | 本次没有重新做可视预览验收 | 精确 Mask、覆盖率、字幕区域及最终 MP4 仍需实测 |
| 旁白与音频 | 本地 TTS 绑定预检存在缺口 | F05；参考音频解析、音频确认和重试 |
| 可选数字人 | 与主流程独立符合当前规范 | 核实创作包字段消费及最终合成行为 |
| 作品输出 | 有自动检查，但本次无新完整视频 | 音频确认门禁、持久作业恢复、实际输出播放 |

不能把本表解读为所有步骤已完成端到端验收。

## 七、建议实施顺序与验收清单

1. 修复 F01/F02：建立“账号 + 当前包 ID → 当前模型 ID → 当前连接”的唯一运行解析路径；禁止无声切回全局模型。
2. 完成 F03/F04：统一删除规则，移除配置历史 API 和不再需要的快照；通过新增迁移处理旧字段和数据。
3. 修复 F05：按实际绑定预检本地服务、工作流、节点和参考音频。
4. 决定 Agent 保留或停用，实施 F06/F07，并同步仓库合同政策。
5. 清理 F08 的文档和无效字段，补实际端到端测试。

交付验收至少包含：两个同类型模型独立修改、多个创作包引用同一模型、修改即时生效、引用中删除被阻止、项目文件损坏不换错模型、跨账号引用被拒绝、IndexTTS 正常和停止两种状态、六个必选步骤完整输出与可选数字人不阻断输出。

## 八、验证记录

此前本次审查已完成：

- 配置、模型、运行配置与 Agent 组合测试：466 passed，1 条 Starlette/TestClient 弃用警告。
- 一键流程、手动暂停及项目服务测试：57 passed。
- `python scripts/generate_agent_contracts.py --check`：通过。

报告发布时另执行仓库 AGENTS.md 的 Required Validation 命令组；最终结果见下方发布核验记录。自动检查用于说明当前基线状态，不作为 F01–F08 已修复的证据。

### 发布核验记录

2026-09-16 已执行 AGENTS.md 的 Required Validation 命令组，结果通过：Python 编译检查、前端语法与可见流程检查、数据库/失效保护/Agent 测试、Agent 合同生成检查、Reveal 与音频完整性检查，以及 Remotion TypeScript 检查均通过。组合 pytest 输出为 421 passed，1 条 Starlette/TestClient 弃用警告；该警告不影响本次报告内容。

## 九、运行环境补充实测

2026-09-16 重新启动本机 ComfyUI 后，`http://127.0.0.1:8188/system_stats` 与 `/object_info` 均返回成功。环境为 NVIDIA GeForce RTX 5080，约 16GB 显存；IndexTTS 2.5 的 Loader、Synthesis、LoadAudio、SaveAudio 四个节点齐全。平台保存的“本地 ComfyUI / IndexTTS 2.5”连接指向该地址，并有已保存的参考音频。

运行目录已有平台端到端烟雾测试产物：`data/integration_smoke/indextts_platform_smoke.wav`。记录显示平台将该参考音频、IndexTTS 工作流和短文本提交到 ComfyUI，得到约 7.941 秒、22,050Hz 单声道 WAV，同时生成字幕和时间轴。因此可以确认“平台 → ComfyUI → IndexTTS 2.5 → 音频产物”的短文本合成链路已跑通。

数字人方面，ComfyUI 已加载 `WanInfiniteTalkToVideo` 等 InfiniteTalk/Wan 节点，且外部运行包内有 Windows 兼容 API 工作流模板。但是平台运行数据目录当前只有 IndexTTS 工作流，缺少数字人 `comfyui_workflow.json` 的已配置副本。因此现阶段只能确认底层节点就绪，不能确认“平台上传形象和音频后可生成数字人视频”。配置该工作流后，应使用一张人物图和 8 秒已确认音频完成独立视频测试，再检查平台作业状态、输出 MP4 与主项目作品输出的衔接。
